import { autoTimeAgo } from 'wge/helpers/auto-time-ago';
import { module, test } from 'qunit';

module('Unit | Helper | auto time ago');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = autoTimeAgo([42]);
  assert.ok(result);
});
