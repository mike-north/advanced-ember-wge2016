import resolveLater from 'wge/utils/resolve-later';
import { module, test } from 'qunit';

module('Unit | Utility | resolve later');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = resolveLater();
  assert.ok(result);
});
